# SmartLab + Wikipedia(Android) Автоматизированные тесты

Проект содержит автоматизированные тесты, разработанные в рамках предемета  «Тестирование сервисов и приложений».

## 📋 Требования

1. Java 11 или выше
2. Maven 3.6 или выше
3. Доступ в интернет

## Структура проекта

```
src/test/java/com/example/testingfinal/
├── mobile/
│   ├── config/                 # конфигурация
│   ├── page/                   # объекты-страницы
│   └── test/                   # тесты
└── web/
    ├── config/                 # конфигурация
    ├── page/                   # объекты-страницы
    └── test/                   # тесты

src/test/resources/
├── mobile-test.properties
└── web-test.properties
```

## 🚀 Установка и запуск

### 1. Клонирование и настройка
```bash
git clone https://github.com/ra9615/finalTesting.git
cd finalTesting
```

### 2. Сборка проекта
```bash
mvn clean install
```

### 3. Запуск всех тестов

```bash
mvn test
```

### Установка Appium 2 и драйвера UiAutomator2
```bash
npm i -g appium
appium driver install uiautomator2
appium --version
```

### Запуск Appium Server
```bash
appium --address 127.0.0.1 --port 4723
```

### Запуск эмулятора и проверка устройства
1. Запустить AVD в Android Studio (Device Manager)
2. Проверить, что устройство видно:
```bash
adb devices
```

## 🧪 Описание тестов

Проект содержит следующие тесты:
## Web:
- 'testPageLoadsSuccessfully' - Проверка загрузки страницы
- testPriceFilter - Фильтрация по цене
- testGenderFilter - Фильтрация по полу
- testBrandFilter - Фильтрация по бренду
- testSortAscending/Descending - Проверка сортировки
- testCombinedFilters - Комбинирование фильтров
- testOpenProductCard - Переход в карточку товара
- testSortByNewest - Сортировка по новизне
- testResetFilters - Сброс фильтров 
## Mobile:
- OpenAppTest — Проверка загрузки приложения
- SearchingTest — Проверка поиска статьи по запросу Java
- OpenArticleTest — Проверка открытия первой найденной статьи
- ScrollingTest — Проверка прокрутки статьи

